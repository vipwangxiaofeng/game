//分数
function createScore(){
  context.fillStyle = '#fff';
  context.strokeStyle = '#fff';
  context.font = '18px 黑体';
  context.fillText('分数:'+score, 20, 20);
  context.strokeText('分数:'+score, 20, 20);
}
